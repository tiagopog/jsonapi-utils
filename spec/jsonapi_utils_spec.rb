require 'spec_helper'

describe JsonapiUtils do
  it 'has a version number' do
    expect(JsonapiUtils::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
